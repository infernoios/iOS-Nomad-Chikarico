import SwiftUI

extension Color {
    static let backgroundPrimary = Color(red: 0.05, green: 0.05, blue: 0.1)
    static let backgroundSecondary = Color(red: 0.1, green: 0.1, blue: 0.15)
    static let cardBackground = Color(red: 0.15, green: 0.15, blue: 0.2)
    static let textPrimary = Color.white
    static let textSecondary = Color(white: 0.75)
    
    // Vibrant gradient colors
    static let gradientStart = Color(red: 0.2, green: 0.1, blue: 0.3)
    static let gradientEnd = Color(red: 0.1, green: 0.2, blue: 0.3)
    
    // Accent colors
    static let accentBlue = Color(red: 0.2, green: 0.6, blue: 1.0)
    static let accentPurple = Color(red: 0.7, green: 0.3, blue: 1.0)
    static let accentPink = Color(red: 1.0, green: 0.3, blue: 0.6)
    static let accentOrange = Color(red: 1.0, green: 0.5, blue: 0.2)
    static let accentGreen = Color(red: 0.2, green: 0.9, blue: 0.5)
}
